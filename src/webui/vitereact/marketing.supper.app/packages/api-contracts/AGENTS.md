# API Contracts Package Rules

- Do NOT edit generated files manually.
- Always run `pnpm api:generate` when `contracts/openapi.yaml` changes.
- Only export contract types and version info from this package.
