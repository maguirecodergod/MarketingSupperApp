# Forms Package Rules

- Form state belongs in TanStack Form + Zod only.
- Map backend FieldError responses cleanly into form field errors.
- Ensure accessible label/description/error association on all fields.
