# UI Package Rules

- NEVER import from `apps/*` or `features/*`.
- Only use Radix primitives and accessible semantic HTML.
- Provide comprehensive states: loading, empty, error, disabled, focus.
- EnterpriseDataGrid must remain decoupled from specific domain models.
